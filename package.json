{
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "start": "next start",
    "export": "next export",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^11.3.0",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
